import { BackHandler, StyleSheet } from 'react-native';

export const gStyle = StyleSheet.create({
    title: {
        fontSize: 28,
        marginTop: 20,
        textAlign: 'center',
        color: '#323232',
        top: 0
    }
});